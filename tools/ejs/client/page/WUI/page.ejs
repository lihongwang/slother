import React from "react";

import classnames from "classnames";

import { BasePage } from "../../COM/mvc";
import Typography from "../../melonUi/src/foundation/layout/MuiTypography";

import <%=pageName%>Action from "./<%=pageName%>Action";
class <%=pageName%>Page extends BasePage {
  constructor(props) {
    super(props);
    this.pageAction = new <%=pageName%>Action({pageId: props.pageId});
    this.pageAction.init(this);
  }

  renderMainContent() {
    if (!this.state.initShow) return <noscript />;
    return (
      <React.Fragment>
        <Typography>Welcome</Typography>
    </React.Fragment>
    );
  }
}
export default <%=pageName%>Page;
