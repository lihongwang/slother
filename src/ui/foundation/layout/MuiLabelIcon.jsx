import { withStyles } from "@material-ui/core/styles";

import { LabelIcon } from "../../core";
const styles = theme => ({
});
class MuiLabelIcon extends LabelIcon {
  
}

export default withStyles(MuiLabelIcon.combineStyles(styles))(MuiLabelIcon);
